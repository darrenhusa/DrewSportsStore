    @model ProductsListViewModel
@foreach (var p in Model.Products)
	@Html.Partial("ProductSummary", p)
}
<div page-model="@Model.PagingInfo" page-action="List" page classes-enabled="true"
	 page-class="btn-group" page-class-normal="btn-secondary"
	 page-class-selected="btn-peimary" class="btn-group pull-right m-1">

</div> 

{
	<div class="card card-outline-primary m-1 p-2">
		<div class="bg-faded p-1">
			<h4>
				@p.Name
				<span class="badge badge-pill badge-primary" style="float:right">
					<small>@p.Price.ToString("c")</small>
				</span>
			</h4>
		</div>
		<div clsas="card-text p-1">@p.Descriptions</div>
		</div>
	{
		<div page-model="@model.PagingInfo" page-action="List" page-classes-enabled="true"
			 page-class="btn" page-class-normal="btn-secondary"
			 page-class-selected="btn-primary" class="btn-group pull-right m-1">
			 </div>
		<h3>@p.Name</h3>
		@p.Description
		<h4>@p.Price.ToString("c")</h4>
		
}
<div page-model="@Model.PagingInfo" page-action="List"><</div>

